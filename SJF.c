#include <stdio.h>

int main(){
   int bt[20], p[20], tat[20], ,i,j,n, total =0, pos, temp;
   float avg_wt, avg_tat;

   printf("Enter the number of process: ");
   scanf("%d", &n);

   printf("Enter the Burst time: \n");
   for (i=0; i< n; i++){
      printf("")
   }
}
